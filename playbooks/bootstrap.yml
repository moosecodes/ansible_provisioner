---

# - hosts: all
#   become: true
#   remote_user: moose
#   roles:
#     - base

# - hosts: servers
#   become: true
#   remote_user: moose
#   roles:
#     - servers

- hosts: workstations
  become: true
  remote_user: "{{ workstation_user_patriot }}"
  roles:
    - role: workstations
      vars:
        workstation_user_patriot: patriot
